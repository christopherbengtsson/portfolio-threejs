export const cursor = document.querySelector('.cursor') as HTMLElement;
export const progressEl = document.querySelector('.progress-percent') as HTMLElement;
export const progressBar = document.querySelector('.progress-circle .line') as HTMLElement;
